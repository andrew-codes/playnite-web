type SiteSetting {
  id: Int!
  name: String!
  value: String!
  dataType: String!
  description: String!
}

extend type Query {
  siteSettings: [SiteSetting!]!
}

extend type Mutation {
  updateSiteSetting(id: Int!, value: String!): SiteSetting!
}
