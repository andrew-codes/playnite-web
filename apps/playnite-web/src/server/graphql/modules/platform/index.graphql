type Platform {
  id: String!
  name: String!
  playniteId: String
  games: [Game!]!
  icon: String
}

extend type Query {
  platform(id: String!): Platform
  platforms(libraryId: String!): [Platform!]!
}
