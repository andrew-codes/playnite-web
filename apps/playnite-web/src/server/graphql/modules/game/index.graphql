type Game {
  id: String!
  primaryRelease: Release
  releases: [Release!]!
  library: Library!
}

input FilterItemInput {
  field: String!
  relatedType: RelatedType
  values: [String!]!
}

input Filter {
  name: String
  filterItems: [FilterItemInput!]
}

extend type Query {
  game(id: String!): Game
  games(libraryId: String!, filter: Filter): [Game!]!
}

# input UpdateGameInput {
#   description: String
#   completionStatusId: String
# }

# extend type Mutation {
#   updateGame(id: String!, input: UpdateGameInput!): BooleanMutationResult!
# }
