type CompletionStatus {
  id: String!
  name: String!
  releases: [Release]!
  playniteId: String
}

extend type Query {
  completionStatus(id: String!): CompletionStatus
  completionStatuses(libraryId: String!): [CompletionStatus!]!
}
