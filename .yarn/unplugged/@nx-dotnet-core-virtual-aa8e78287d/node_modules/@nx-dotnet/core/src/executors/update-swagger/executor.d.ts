import { ExecutorContext } from '@nx/devkit';
import { DotNetClient } from '@nx-dotnet/dotnet';
import { UpdateSwaggerJsonExecutorSchema } from './schema';
export declare const SWAGGER_CLI_TOOL = "Swashbuckle.AspNetCore.Cli";
export default function runExecutor(schema: Partial<UpdateSwaggerJsonExecutorSchema>, context: ExecutorContext, dotnetClient?: DotNetClient): Promise<{
    success: boolean;
}>;
