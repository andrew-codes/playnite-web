import { ExecutorContext } from '@nx/devkit';
import { DotNetClient } from '@nx-dotnet/dotnet';
import { PublishExecutorSchema } from './schema';
export default function runExecutor(options: PublishExecutorSchema, context: ExecutorContext, dotnetClient?: DotNetClient): Promise<{
    success: boolean;
}>;
