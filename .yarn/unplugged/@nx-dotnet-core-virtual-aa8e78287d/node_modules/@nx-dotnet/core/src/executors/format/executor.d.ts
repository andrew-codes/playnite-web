import { ExecutorContext } from '@nx/devkit';
import { DotNetClient } from '@nx-dotnet/dotnet';
import { FormatExecutorSchema } from './schema';
export default function runExecutor(options: FormatExecutorSchema, context: ExecutorContext, dotnetClient?: DotNetClient): Promise<{
    success: boolean;
}>;
