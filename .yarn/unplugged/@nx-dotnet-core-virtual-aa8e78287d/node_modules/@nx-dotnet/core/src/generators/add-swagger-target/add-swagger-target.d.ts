import { Tree } from '@nx/devkit';
import { AddSwaggerJsonExecutorSchema } from './schema';
export default function generateSwaggerSetup(host: Tree, options: AddSwaggerJsonExecutorSchema): Promise<() => Promise<void>>;
