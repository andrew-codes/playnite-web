import { Tree } from '@nx/devkit';
import { DotNetClient } from '@nx-dotnet/dotnet';
import { NormalizedSchema } from './generate-project';
export interface PathParts {
    suffix: string;
    separator: '.' | '-';
}
export declare function GenerateTestProject(host: Tree, schema: NormalizedSchema, dotnetClient: DotNetClient): Promise<void>;
