import { Tree } from '@nx/devkit';
import { DotNetClient } from '@nx-dotnet/dotnet';
export default function (host: Tree, options: null, // The second option is provided at runtime by Nx for options passed in to the generator.
dotnetClient?: DotNetClient): Promise<() => Promise<void>>;
